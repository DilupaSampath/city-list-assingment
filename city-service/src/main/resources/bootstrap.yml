spring:
  application:
    name: CITY-SERVICE
server:
  port: 8093
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8760/eureka/
  instance:
    hostname: localhost